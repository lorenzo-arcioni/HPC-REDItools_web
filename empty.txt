Campi da aggiungere:

1) --bed_file [PATH]: Path of BED file containing target self.regions
2) --splicing-file [PATH]: The file containing the splicing sites positions
3) --load-omopolymeric-file [PATH]: The file containing the omopolymeric positions
4) --create-omopolymeric-file [PATH]: Path to write omopolymeric positions to
5) --reference [PATH] : Path to the reference genome

Discutere se è meglio eliminare tutti i campi e tenere una text area dove l'utente inserisce tutte le options
oppure se conviene mettere tutti i campi e vincolare un po di cose.

# Discutere se è possibile eseguire ulteriori test utilizzando i 4 file aggiuntivi (quelli qui sopra),
  e come, eventualmente, gestire le posizioni e l'utilizzo di file diversi per ogni bam (se serve).

# Discutere il setting dell'interfaccia in particolare cio che riguarda la condivisione e l'installazione del software.
